# User Database App

Приложение базы пользователей с авторизацией, разработанное согласно тестовому заданию.

## �️ Функциональность

- **Авторизация**: Страница логина с валидацией форм
- **Список пользователей**: Отображение списка пользователей с infinite scroll (подгрузка при достижении конца списка)
- **Создание пользователя**: Модальное окно для создания нового пользователя с валидацией
- **Редактирование пользователя**: Модальное окно для редактирования существующего пользователя
- **Удаление пользователя**: Удаление пользователя с подтверждением
- **404 страница**: Обработка несуществующих маршрутов
- **Защищенные маршруты**: Автоматическая редирекция неавторизованных пользователей на страницу логина

## � Технологический стек

- **React** 16.14.0
- **TypeScript** 4.9.5
- **Webpack** 5.89.0 + **Npm**
- **Ant Design** 5.12.0 (UI компоненты)
- **styled-components** 6.1.0 (кастомные стили)
- **React Router** 6.8.0 (маршрутизация)
- **TanStack Query** 4.36.1 (управление состоянием и кэширование данных)
- **Axios** 1.6.0 (HTTP клиент)
- **Day.js** 1.11.10 (работа с датами)

### Инструменты разработки

- **ESLint** 8.57.1 (линтинг кода)
- **Prettier** 3.8.1 (форматирование кода)
- **Jest** 29.7.0 (тестирование)
- **React Testing Library** 12.1.5 (тестирование компонентов)

## 📁 Архитектура

Проект организован по методологии **Feature-Sliced Design (FSD)**:

```
src/
├── app/                    # Инициализация приложения
│   ├── App.tsx              # Корневой компонент
│   ├── router.tsx             # Конфигурация маршрутов
│   └── styles.css            # Глобальные стили
│
├── pages/                   # Страницы приложения
│   ├── login/                # Страница авторизации
│   ├── users/                 # Страница списка пользователей
│   └── not-found/           # Страница 404
│
├── features/                 # Функциональные возможности
│   ├── auth/                 # Авторизация
│   │   ├── api/               # API запросы
│   │   └── model/             # Бизнес-логика (хуки)
│   └── user-management/        # Управление пользователями
│       └── ui/                # UI компоненты
│
├── entities/                 # Бизнес-сущности
│   └── user/                 # Сущность "Пользователь"
│       ├── api/               # API запросы
│       ├── lib/               # Сервисы и утилиты
│       ├── model/             # Бизнес-логика (хуки)
│       └── ui/                # UI компоненты
│
└── shared/                   # Переиспользуемые компоненты
    ├── lib/                   # Библиотеки и конфигурации
    ├── ui/                   # UI компоненты
    ├── utils/                 # Утилиты
    └── types/                 # TypeScript типы
```

## � Установка и запуск

### Предварительные требования

- Node.js (рекомендуется v18+)
- Npm или Yarn

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd i-teko
```

2. Установите зависимости:
```bash
npm install
```

### Запуск

**Режим разработки:**
```bash
npm start
```

Приложение будет доступно по адресу `http://localhost:3000` (или другому порту, если 3000 занят).

**Сборка для продакшена:**
```bash
npm run build
```

Собранные файлы будут находиться в директории `dist/`.

## � Скрипты

| Скрипт | Описание |
|---------|----------|
| `npm start` | Запуск dev-сервера с hot-reload |
| `npm run build` | Сборка проекта для продакшена |
| `npm run type-check` | Проверка типов TypeScript |
| `npm test` | Запуск unit тестов |
| `npm run test:watch` | Запуск тестов в режиме watch |
| `npm run test:coverage` | Запуск тестов с покрытием кода |
| `npm run lint` | Проверка кода линтером |
| `npm run lint:fix` | Автоматическое исправление ошибок линтера |
| `npm run format` | Форматирование всех файлов Prettier |
| `npm run format:check` | Проверка форматирования без изменений |

## � Тестирование

Проект использует **Jest** и **React Testing Library** для unit тестирования:

- Тесты компонентов (`*.test.tsx`)
- Тесты хуков (`*.test.tsx`)
- Тесты сервисов (`*.test.ts`)

**Запуск тестов:**
```bash
npm test
```

**Запуск тестов в режиме watch:**
```bash
npm run test:watch
```

**Покрытие кода тестами:**
```bash
npm run test:coverage
```

## 🔍 Линтинг и форматирование

Проект настроен с **ESLint** и **Prettier** для поддержания качества кода.

**Проверка кода:**
```bash
npm run lint
```

**Автоматическое исправление:**
```bash
npm run lint:fix
```

**Форматирование кода:**
```bash
npm run format
```

**Проверка форматирования:**
```bash
npm run format:check
```

## 🌐 API

Приложение использует API из **mockapi.io**:

- **Базовый URL**: `https://698cc4ca21a248a273628e82.mockapi.io`
- **Endpoint для пользователей**: `/users`

### Структура данных пользователя

```typescript
{
  id: string;
  name: string;
  surname: string;
  email: string;
  age: number;
  createdAt: string;
}
```

## 🔐 Авторизация

Для входа в систему используйте:

- **Логин**: `admin`
- **Пароль**: `admin`

После успешной авторизации токен сохраняется в `localStorage`, и пользователь автоматически редиректится на страницу списка пользователей.

## � Разработка

### Рабочий процесс

Разработка ведется итеративно: каждый этап реализуется в отдельной ветке Git с последующим мерджем в master ветку.

### Конвенции кода

- **TypeScript**: Строгая типизация, избегание `any` где возможно
- **Компоненты**: Функциональные компоненты с TypeScript
- **Стили**: Ant Design для базовых компонентов, styled-components для кастомных стилей
- **Имена**: camelCase для переменных и функций, PascalCase для компонентов
- **Файлы**: Один компонент/хук на файл

### Структура коммита

```
type(scope): subject

body

footer
```

Пример:
```
feat(users): add infinite scroll

Implement infinite scroll functionality for user list
using TanStack Query useInfiniteQuery hook
```

## 📦 Лицензия

Приватный проект.

## 👤 Автор

Разработанно согласно тестовому заданию.
